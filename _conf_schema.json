{
    "monitor_timeout": {
        "description": "监控超时时间",
        "hint": "监控指令任务响应的超时时间（秒）",
        "type": "int",
        "slider": {
            "min": 1,
            "max": 600,
            "step": 1
        },
        "default": 60
    }
}